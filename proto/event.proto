syntax = "proto3";

package event;

service EventService {
    rpc FindOne (FindOneRequest) returns (Event);
}

message FindOneRequest {
    string event_id = 1;
}

message Event {
    string title = 1;
    string type = 2;
}
